import { useEffect, useState } from 'react'
import { getMe, logout } from '../../services/api'
import './DashboardPage.css'
export default function DashboardPage(){const [user,setUser]=useState(null);useEffect(()=>{getMe().then(r=>setUser(r.data.user)).catch(()=>{localStorage.removeItem('workman_token');window.location.href='/login'})},[]);const signOut=async()=>{await logout().catch(()=>{});localStorage.removeItem('workman_token');window.location.href='/'};return <main className="dashboard"><header className="dash-nav"><a className="brand" href="/"><span className="brand-mark">W</span>workman<span className="brand-dot">.</span></a><button onClick={signOut}>Sign out</button></header><section className="dash-welcome"><div className="eyebrow"><span className="eyebrow-line"/> YOUR WORKMAN SPACE</div><h1>Hey{user?`, ${user.name.split(' ')[0]}`:''}.<br/><em>What’s next?</em></h1><p>Your trusted local help, all in one place.</p><div className="empty-panel"><div className="empty-icon">✦</div><h2>Your first job starts here.</h2><p>Tell us what you need and we’ll help you find the right professional nearby.</p><a className="button button-dark" href="/#services">Browse services <span>↗</span></a></div></section></main>}
